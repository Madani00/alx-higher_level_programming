#!/usr/bin/node

// prints 3 lines: but by using an array of string and a loop “JavaScript is amazing”
const myVars = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
for (let i = 0; i < myVars.length; i++) {
  console.log(myVars[i]);
}
